import { StyleSheet} from "react-native";

const style=(width,height,isPortrait)=>StyleSheet.create({
   touch:{paddingLeft:15},
   image:{
    height:25,
    width:25,
    resizeMode:'contain'}
    }
)
export default style;